{
  "name": "piw-pew",
  "scripts": {
    "client:dev": "RUST_LOG=TRACE cargo run --package client",
    "server:dev": "RUST_LOG=TRACE cargo run --package server",
    "client:build": "cargo build --release --package client",
    "server:build": "cargo build --release --package server",
    "build": "cargo build --release"
  }
}
